<div class="section">
    <h2>Skills</h2>
    {{#each items}}
    <div class="subsection">
        <h3>{{category}}</h3>
        <div class="tags">
            {{#each tags}}
            <span class="cvtag">{{this}}</span>
            {{/each}}
        </div>
    </div>
    {{/each}}
</div>